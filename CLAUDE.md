# CLAUDE.md

このリポジトリ（`walover-line-harness-kit`）で作業する Claude Code 向けの指示書です。

WALOVER のプロジェクトでは、Claude Code への指示は
**「CLAUDE.md を読んでから作業してください」**から始める運用にしています。
このファイルを読んだら、以下のルールに従って作業してください。

---

## 1. このリポジトリは何か / 何ではないか

WALOVER 勉強会の参加者が、**参加者自身の Cloudflare アカウント・参加者自身の LINE 公式アカウント**で
LINE Harness を立ち上げるための、**手順書とサポート情報を置くリポジトリ**です。

| | |
|---|---|
| **ここにあるもの** | セットアップ手順書 / サポートポリシー / 動作検証の記録（`docs/findings/`） |
| **ここに無いもの** | LINE Harness 本体のコード |

- ツール本体は WALOVER の成果物ではありません。本家 [Shudesu/line-harness-oss](https://github.com/Shudesu/line-harness-oss)（MIT）が公開する
  `npx create-line-harness@latest setup` を**そのまま**使います。
- WALOVER が提供するのは、**その1コマンドの前後で必要になる LINE 側の設定手順**と、
  **セットアップでつまずいたときのサポート**だけです。
- **成果物はドキュメントのみです。** このリポジトリにアプリケーションコードは存在せず、追加もしません。

### 別リポジトリとの関係

- **本家**: `Shudesu/line-harness-oss` — CLI とアプリ本体。配布するのはこれ。
- **WALOVER Fork**: `saniman/line-harness-oss` — **既存顧客専用・配布対象外**。このリポジトリとは完全に別物です。

---

## 2. 作業を始める前に必ず読むファイル

1. `docs/plan.md` — 判断の記録、スコープ、**Issue 一覧（#1〜#4）・SP・依存グラフ**、未決事項
2. `docs/findings/` 配下の既存ファイル — #1 以降の実測記録。手順書を書く前提になります
3. `README.md` — 参加者から見えている現在の説明。ここと矛盾する記述を他所に書かない

`docs/findings/` がまだ空、または存在しない場合は、**#1（実測）が未着手**ということです。
手順書（#3）の内容を実測なしで書き始めないでください。

---

## 3. やってはいけないこと（禁止事項）

### 3.1 シークレットを一切書かない

LINE Channel Secret / Channel Access Token、Cloudflare API トークン、その他 API キーなどの
**実際の値を、このリポジトリのどこにも残さない**でください。

- 対象は「コード」だけではありません。**README・手順書・`docs/findings/` の記録・Issue 本文・Issue のコメント**、すべて含みます。
- 手順書に例示が必要な場合は、必ず `<あなたのチャネルシークレット>` のような**プレースホルダ**にします。
- 実測ログやスクリーンショットを `docs/findings/` に貼る場合は、**貼る前にマスクする**。
  マスクせずに一度コミットしたら、履歴に残るため取り返しがつきません。
- 参加者からシークレットが送られてきた場合も、このリポジトリに転記しない。

WALOVER は参加者のシークレットを預かりません。この方針を崩す記述・仕組みを提案しないでください。

### 3.2 Fork 由来の情報を混入させない

WALOVER Fork（`saniman/line-harness-oss`）の以下は、**このリポジトリに絶対に持ち込まない**でください。

- `wrangler.toml` の内容全般
- D1 データベース ID / Cloudflare アカウント ID
- freee の取引先 ID、Stripe / Google Calendar 等の連携設定
- Fork 固有機能（イベント予約 + Stripe 決済 / freee 領収書自動化 / モバイルオーダー / AI 週次配信）の説明や設定値

リポジトリを分けている目的のひとつが「参加者が Fork に触れる経路を断つこと」です。
手順書から Fork を参照させることもしないでください。

### 3.3 本家のコードを修正しない

本家 CLI やアプリの不具合を見つけても、**このリポジトリ内で修正・パッチ・回避コードを作らない**でください。

- やること: `docs/findings/` に**事象として記録**し、**本家へ Issue 報告を促す記述**に留める
- やらないこと: 修正パッチ、fork してのビルド、独自ラッパー CLI の作成

参加者向けの記述も同様で、「本家のコードに起因する不具合は WALOVER では修正できず、
[本家リポジトリ](https://github.com/Shudesu/line-harness-oss/issues) へ報告いただく」形を維持します。

### 3.4 `npx create-line-harness` の中身を改変しない

配布するのは**本家の CLI をそのまま**です。

- CLI のラップ、フォーク、パッチ適用、独自バージョンの publish はしない
- 手順書では `npx create-line-harness@latest setup` を**素のまま**案内する
- CLI が自動化していない部分（LINE Developers 側の設定など）を手順で埋めるのが、このリポジトリの役割です

---

## 4. 進め方

### 4.1 Issue 駆動

作業は必ず `docs/plan.md` の Issue（#1〜#4）に紐づけて進めます。
どの Issue にも属さない作業を思いつきで始めないでください。必要なら先に Issue を立てます。

### 4.2 SP3 を超えるタスクは分解してから着手する

WALOVER 本体のプロジェクトと同じルールです。

- 目安: **SP1 = 半日（2〜3時間） / SP2 = 1日 / SP3 = 2日**
- SP3 を超えると見積もったタスクは、**着手前に SP3 以下へ分解**する
- 分解の結果は `docs/plan.md` の Issue 一覧に反映する

### 4.3 依存関係を確認し、前提が終わっていないタスクに着手しない

`docs/plan.md` の進行表・依存グラフを毎回確認してください。

```
#1（調査・SP2）─┐
                 ├── #3（手順書・SP3）── #4（第三者検証・SP3）
#2（ポリシー・SP2）─┘
```

- `#1` と `#2` は独立 — 並行して進められる
- `#3` は `#1` と `#2` の**両方**が完了していることが前提
- `#4` は `#3` が完了していることが前提

前提の Issue が未完了のタスクを頼まれた場合は、**着手せずに依存関係を指摘**してください。

---

## 5. 文書を書くときの前提

- 読者は**非開発者の勉強会参加者**です。専門用語には補足をつけ、コマンドはコピペで動く形にします。
- 記述は `README.md` / `docs/plan.md` / `docs/findings/` の間で矛盾させない。片方を直したら他方も確認する。
- サポート範囲（**配布日から3ヶ月・セットアップに関するもののみ・LINE グループでの質問対応のみ**）を
  勝手に広げる記述をしない。変更は #2 の決定事項として扱う。
- 未決事項（ライセンス表記、勉強会日程、配布方法、LINE グループ導線）は `docs/plan.md` の「5. 未決の項目」で管理します。
  勝手に決めず、決まったらそこを更新してください。
